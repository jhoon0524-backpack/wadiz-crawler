name: Crawler 검증 (임시)

on:
  push:
    branches:
      - claude/wadiz-crawler-issue-kkvmdv

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python crawler.py
